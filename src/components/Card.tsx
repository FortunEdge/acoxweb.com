/** 
 * The base component for organization of info site and portfolio.
 * Encapsulates the display in Card
 */
import {Card as MuiCard, CardContent} from '@mui/material';

export interface CardProps {

}

const Card = () => {
    return (
        <MuiCard>
            <CardContent>
                
            </CardContent>
        </MuiCard>
    )
}

export default Card;